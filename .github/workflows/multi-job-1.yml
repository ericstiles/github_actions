name: job1
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀 for job1
on: [push]
jobs:
  Explore-GitHub-Actions:
    if: true
    runs-on: ubuntu-latest
    steps:
      - run: env | sort